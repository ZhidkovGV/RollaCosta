import { TotalWalletBalancePipe } from './total-wallet-balance.pipe';

describe('TotalWalletBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalWalletBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
